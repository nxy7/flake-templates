# Produkcja/staging na VPS. Katalog na serwerze: /opt/app (kopiowany przez CI).
# Sekrety w /opt/app/.env (zapisuje CI z GitHub Secrets; wzór: deploy/.env.example).
name: app

x-api: &api
  image: ${API_IMAGE:?ustaw API_IMAGE}:${API_TAG:-latest}
  env_file: .env
  environment:
    NODE_ENV: production
    DATABASE_URL: postgres://app:${POSTGRES_PASSWORD:?}@db:5432/app
  depends_on:
    db:
      condition: service_healthy

services:
  api:
    <<: *api
    restart: unless-stopped
    expose: ["3000"]

  migrate:
    <<: *api
    command: ["bun", "migrate-cli.js"]
    restart: "no"
    profiles: ["tools"]

  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?}
      POSTGRES_DB: app
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 5s
      timeout: 3s
      retries: 20

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports: ["80:80", "443:443", "443:443/udp"]
    environment:
      API_DOMAIN: ${API_DOMAIN:?}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on: [api]

  backup:
    image: ${BACKUP_IMAGE:?ustaw BACKUP_IMAGE}:${API_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgres://app:${POSTGRES_PASSWORD:?}@db:5432/app
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
  caddy_data:
  caddy_config:
